import React from 'react';
import Header from './sub-components/Header';

const Main: React.FC = () => {
   return (
      <div style={{ height: '100vh' }}>
         <Header />
      </div>
   );
};

export default Main;
